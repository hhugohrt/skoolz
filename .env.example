VITE_API_URL=http://localhost:4010

# Connexion avec Google : ID client OAuth "Application Web" (Google Cloud Console > API et services > Identifiants).
# Identifiant public, à mettre aussi dans GOOGLE_CLIENT_ID côté API. Sans lui, le bouton Google est masqué.
VITE_GOOGLE_CLIENT_ID=
